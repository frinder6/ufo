<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ufo.mapper.impl.GridInfoEntityMapperImpl">
    <resultMap id="BaseResultMap" extends="com.ufo.mapper.GridInfoEntityMapper.BaseResultMap"
               type="com.ufo.entity.GridInfoEntity">
    </resultMap>
    <sql id="Base_Column_List">
        <include refid="com.ufo.mapper.GridInfoEntityMapper.Base_Column_List"/>
    </sql>
    <select id="selectByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from ufo_grid_info
        where name = #{name,jdbcType=BIGINT}
    </select>
    <select id="selectValidList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from ufo_grid_info
        where status = 1
    </select>

    <sql id="Base_Where_Clause">
        <if test="e.name != null">
            AND `name` LIKE #{e.name,jdbcType=VARCHAR}"%",
        </if>
        <if test="e.status != null">
            AND `status` = #{e.status,jdbcType=BIT},
        </if>
        <if test="e.createTime != null">
            AND create_time = #{e.createTime,jdbcType=TIMESTAMP},
        </if>
        <if test="e.creater != null">
            AND creater = #{e.creater,jdbcType=VARCHAR},
        </if>
        <if test="e.updateTime != null">
            AND update_time = #{e.updateTime,jdbcType=TIMESTAMP},
        </if>
        <if test="e.updater != null">
            AND updater = #{e.updater,jdbcType=VARCHAR},
        </if>
    </sql>
    <!-- 分页查询 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM ufo_grid_info
        WHERE 1 = 1
        <include refid="Base_Where_Clause"/>
        ORDER BY ${p.sort} ${p.order}
        LIMIT #{p.pageFrom}, #{p.pageSize}
    </select>
    <select id="selectPageCount" resultType="java.lang.Integer">
        SELECT
        COUNT(1)
        FROM ufo_grid_info
        WHERE 1 = 1
        <include refid="Base_Where_Clause"/>
    </select>

</mapper>